# Claude Instructions

Follow the repository guidance in `AGENTS.md`.

This repo is a data-only Marble Skill Taxonomy release. Default to small,
careful edits; preserve dataset IDs, graph integrity, manifest checksums, and
the licensing/provenance boundaries in `README.md` and `PROVENANCE.md`.

Before handing off work, run:

```bash
npm run validate
```

If validation cannot be run, say why.
